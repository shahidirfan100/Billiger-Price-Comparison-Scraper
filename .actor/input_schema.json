{
  "title": "Billiger Price Comparison Scraper",
  "description": "Scrape product prices and offers from Billiger.de, Germany's leading price comparison platform. Search by product name or provide direct product URLs.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Product URL",
      "type": "string",
      "description": "Direct Billiger.de product or pricelist URL to scrape.",
      "editor": "textfield",
      "pattern": "^https?://.*billiger\\.de.*$"
    },
    "searchQuery": {
      "title": "Search Query",
      "type": "string",
      "description": "Product name or keywords to search for on Billiger.de.",
      "editor": "textfield",
      "prefill": "laptop"
    },
    "results_wanted": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Maximum number of products to scrape.",
      "minimum": 1,
      "maximum": 1000,
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "collectOffers": {
      "title": "Collect Individual Offers",
      "type": "boolean",
      "description": "Extract individual shop offers with prices for each product.",
      "editor": "checkbox",
      "default": true
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings. Residential proxies recommended for best results.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": [],
  "additionalProperties": false
}